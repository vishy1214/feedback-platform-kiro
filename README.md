# AI Feedback Platform - Minimal Skeleton Repo

## Structure
- `client/` - React 18 frontend
- `server/` - Backend skeleton (FastAPI or Node.js alternative)
- `docker-compose.yml` - To run both services

## Run
```bash
docker-compose up --build
```
